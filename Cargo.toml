[package]
name = "dvrepair"
version = "0.1.0"
edition = "2021"
description = "DV tape repair tool - conjoin multiple captures of the same tape"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1"
log = "0.4"
env_logger = "0.11"